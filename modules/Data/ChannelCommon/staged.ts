export type MorphingChannel = {

};

/**
 * Given a set of "channel handlers",
 * await a special "init" object that is
 * used to pick a specific channel
 * handler to "transform" the channel into.
 */
export const createStagedChannel = () => {
  const onInitialization = () => {

  };

};
